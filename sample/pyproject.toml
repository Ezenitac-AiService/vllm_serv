[project]
name = "vllm-serv-samples"
version = "0.1.0"
description = "vllm_serv AI 서비스 개발자 양성과정 교육용 예제 실습 수트"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
    "openai>=1.0.0",
    "pydantic>=2.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
