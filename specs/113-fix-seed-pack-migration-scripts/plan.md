# Implementation Plan: 시드 팩 마이그레이션 파이프라인 및 ProcessManager 호환성 전수 검증 (Fix Seed Pack Migration Pipeline & ProcessManager Compatibility)

**Branch**: `113-fix-seed-pack-migration-scripts` | **Date**: 2026-08-08 | **Spec**: [`specs/113-fix-seed-pack-migration-scripts/spec.md`](file:///home/dev/storage/vllm_serv/specs/113-fix-seed-pack-migration-scripts/spec.md)

**Input**: Feature specification from `/specs/113-fix-seed-pack-migration-scripts/spec.md`

## Summary

타 시스템 및 이종 플랫폼으로 시드 팩을 이관(`make_seed_pack.sh` -> `unpack_seed.sh` -> `./setup.sh` -> `benchmark_context_window.py`)하는 전체 파이프라인에서 핵심 스크립트 누락이나 `AttributeError` 예외 없이 100% 정상 완수되도록 전수 무결성 검사 및 `ProcessManager` 이중 하위 호환 구조를 구축합니다.

---

## Technical Context

**Language/Version**: Python 3.12, Bash (POSIX compliant shell scripts)

**Primary Dependencies**: `uv`, `pytest`, `fastapi`, `llama-cpp-python`, `pydantic`

**Storage**: Local Filesystem, POSIX Tarball (`.tar.gz`), ZIP Archive (`.zip`), SQLite (`data/metrics.db`)

**Testing**: `pytest` (`uv run pytest tests/unit/`), Bash script testing (`bash -n`)

**Target Platform**: Linux (Ubuntu/Debian, CentOS/RHEL) x86_64, NVIDIA CUDA environment

**Project Type**: Python & Bash Server Infrastructure & Migration Automation CLI Tool

**Performance Goals**: Seed pack archive generation < 10s, Unpack & Verification < 5s, Zero runtime `AttributeError`

**Constraints**: Non-destructive extraction (preserve existing binaries), Double safety net for `ProcessManager` static & instance method calls

**Scale/Scope**: 12 candidate LLM benchmark support, multi-platform migration pack pipeline

---

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- [x] 계획서가 한국어로 작성되었는가? (언어 정책)
- [x] 테스트 코드 작성 계획이 포함되어 있는가? (테스트 필수 원칙)
- [x] 목업은 유료/제한 API로 엄격히 제한하고 실물 시스템/소켓/OS 인자 및 실제 호출 플래그(REAL_API_CALL=1) 기반 실측 검증 계획이 포함되어 있는가? (실체적 테스트 및 수렴 검증 원칙)
- [x] 작업의 종료 조건(Definition of Done)이 명확히 정의되었는가? (종료 조건 명확화 원칙)
- [x] 비파괴적 문서 수정 원칙을 준수하는가? (비파괴적 문서 수정 원칙)
- [x] uv 패키지 매니저 및 가상환경 격리 표준(uv run)을 준수하는가? (uv 패키지 및 환경 관리 원칙)
- [x] 전체 회귀 테스트 수트 및 Playwright 기반 E2E 브라우저 실측 검증 계획이 포함되어 있는가? (의무적 회귀 테스트 및 브라우저 E2E 검증 원칙)

---

## Project Structure

### Documentation (this feature)

```text
specs/113-fix-seed-pack-migration-scripts/
├── plan.md              # Implementation Plan (this file)
├── research.md          # Technical research & decisions (Phase 0)
├── data-model.md        # Interface contracts & pipeline schema (Phase 1)
├── quickstart.md        # Runnable validation guide (Phase 1)
├── checklists/
│   └── requirements.md  # Specification Quality Checklist
└── tasks.md             # Task breakdown (generated by /speckit-tasks)
```

### Source Code Layout

```text
src/
└── core/
    ├── process_manager.py        # ProcessManager static & instance dual-compatibility
    ├── model_downloader.py       # Model downloader & VRAM pre-check helpers
    ├── gpu_detector.py           # Hardware auto-detection
    └── llama_manager.py          # Llama-server daemon manager

scripts/
├── make_seed_pack.sh             # Seed pack archiver with expanded manifest checks
├── unpack_seed.sh                # Non-destructive unpacker with pre/post verify
├── setup.sh                      # One-stop environment setup & Step 1 fail-fast checks
├── benchmark_context_window.py   # Context window binary search with defensive error handling
├── benchmark_quality.py          # Quality evaluator benchmark runner
├── start_server.sh               # Server daemon startup helper
├── stop_server.sh                # Server daemon shutdown helper
└── status_server.sh              # Server status reporting helper

tests/
└── unit/
    ├── test_process_manager.py   # ProcessManager dual-compatibility unit tests
    ├── test_seed_pack.py         # Seed pack archive creation & extraction tests
    └── test_benchmark_context.py # Benchmark script exception handling tests
```

**Structure Decision**: Single Python/Bash project architecture. Modifying core utilities in `src/core/`, pipeline scripts in `scripts/`, and unit tests in `tests/unit/`.

---

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

*(위반 없음 - 헌장 게이트 100% 준수)*
